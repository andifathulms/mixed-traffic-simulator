node_modules
dist
dist-ssr
coverage
*.local
.DS_Store
.vite

# Brand source package. Only the handful of files the app actually
# serves are committed, under public/.
exports

# TypeScript incremental build state. Machine-local, regenerated by
# `tsc -b`, and committed here by accident in an earlier change.
*.tsbuildinfo
